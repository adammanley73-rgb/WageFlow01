// app/dashboard/payroll/page.tsx
// Payroll page using the shared PageTemplate with gold-standard UI.

import PageTemplate, { StatTile, ActionTile } from '@/components/ui/PageTemplate'

export const revalidate = 0

export default function PayrollPage() {
  return (
    <PageTemplate title="Payroll" currentSection="payroll">
      {/* Stat tiles */}
      <div className="mt-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <StatTile title="Open Runs" value="0" />
        <StatTile title="Processing" value="0" />
        <StatTile title="Approved" value="0" />
        <StatTile title="RTI Submitted" value="0" />
      </div>

      {/* Actions */}
      <div className="mt-6 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <ActionTile
          title="Payroll Wizard"
          href="/dashboard/payroll/new"
          description="Create a new payroll run"
        />
        <ActionTile
          title="Schedules"
          href="/dashboard/payroll/schedules"
          description="Pay frequencies and periods"
        />
        <ActionTile
          title="Pay Elements"
          href="/dashboard/payroll/pay-elements"
          description="Earnings, deductions, rates"
        />
        <ActionTile
          title="Batch"
          href="/dashboard/payroll/batch"
          description="Bulk actions and exports"
        />
      </div>

      {/* Info block */}
      <div className="mt-8 rounded-2xl bg-white/95 shadow-sm ring-1 ring-neutral-300 p-5">
        <h2 className="text-lg font-bold text-neutral-900">Workflow status</h2>
        <p className="mt-2 text-sm text-neutral-600">
          Runs progress through draft → processing → approved → RTI submitted → completed.
          Counts will be wired from Supabase.
        </p>
      </div>
    </PageTemplate>
  )
}
