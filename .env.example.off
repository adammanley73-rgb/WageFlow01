# Preview build profile
BUILD_PROFILE=preview

# Public flags
NEXT_PUBLIC_APP_NAME=WageFlow
NEXT_PUBLIC_ENV=preview

# Disable telemetry in CI/preview
NEXT_TELEMETRY_DISABLED=1

# Placeholders for future real integrations (unused in preview stubs)
SUPABASE_URL=
SUPABASE_ANON_KEY=
