# Local prod-like run. Lets you test middleware auth on your machine.
# Remove before committing if your repo is public.

VERCEL_ENV=production

# Basic auth credentials used by middleware in production
AUTH_USER=admin
AUTH_PASS=change-me-now
AUTH_REALM=WageFlow
